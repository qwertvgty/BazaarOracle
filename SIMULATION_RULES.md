# Combat Simulation Rules

这份文档记录当前已确认的《The Bazaar / 大巴扎》战斗规则，用作插件模拟器的实现基线。
后续如果日志验证发现偏差，应先更新本文件，再更新代码。

## Time Base

- 战斗时间最小离散单位是 `50ms`。
- 所有基于时间的效果都应按 `50ms` 为最小步长计算。
- 物品伤害数值按整数处理。

## Cooldown

- 每个物品有自己的冷却时间。
- 时间正常流逝 `1s` 时，默认物品冷却减少 `1s`。
- 同时存在加速和减速时，时间按正常速度计算，也就是回到默认 `1x` 冷却流逝。

## Haste / 加速

- 加速不是“直接减去固定冷却值”，而是让冷却流逝速度增加。
- 加速 `100%` 时：
  时间经过 `1s`，物品冷却减少 `2s`。
- 加速效果本质上是提升冷却结算倍率。

## Slow / 减速

- 减速不是“直接增加固定冷却值”，而是让冷却流逝速度降低。
- 减速 `100%` 时：
  时间经过 `1s`，物品冷却只减少 `0.5s`。
- 减速效果本质上是降低冷却结算倍率。

## Freeze / 冻结

- 冻结期间，物品冷却值不会变化。
- 但冻结期间，加速和减速效果的持续时间仍然正常流逝。
- 如果冻结结束后仍有加速或减速剩余，再按剩余倍率继续结算冷却。

## Burn / 灼烧

- 灼烧每 `500ms` 结算一次。
- 每次结算时，按当前灼烧值造成伤害。
- 每次结算后，灼烧衰减 `3%`。
- 每次衰减至少减少 `1` 点。
- 因此灼烧会随时间自然衰减，但不会卡在高层数不掉。

## Poison / 中毒

- 中毒每 `1s` 结算一次。
- 每次结算时，按当前中毒值造成伤害。
- 中毒不会自然衰减。

## Heal / 治疗

- 治疗回复生命值，但不能超过最大生命值。
- 每次治疗会同时减少 `5%` 治疗数值的毒。
- 每次治疗会同时减少 `5%` 治疗数值的火。
- 这里的“毒”和“火”分别对应中毒和灼烧层数。

## Shield / 护盾

- 护盾优先承受伤害。
- 只有护盾被扣完后，剩余伤害才会作用到生命值。

## Damage / 伤害

- 伤害最终按整数结算。
- 在同一个时间步里，具体事件顺序如果影响结果，需要在实现里明确并保持一致。

## Implementation Notes

- 当前模拟器还没有完全对齐这份规则，尤其是：
  - 加速 / 减速目前仍有简化
  - 冻结与加减速的交互需要按本规则重做
  - 灼烧和中毒的结算周期需要按 `500ms / 1s` 明确拆分
  - 治疗清毒清火尚未完整接入
- 任何新确认的机制，先补到本文件，再改代码。
